<nav class="navbar navbar-dark navbar-custom" role="navigation">
  <div class="container">
    <div class="navbar-header">
     <a class = "navbar-brand" href = "/movies">
     Movie Reviews
     </a>
   </div>
    <div>
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li ><%= link_to "Add Movie", new_movie_path, class: "active" %></li>
          <li><%= link_to "Account", edit_user_registration_path %></li>
        <% else %>
          <li><%= link_to "Login", new_user_session_path, class: "active" %></li>

        <% end %>
      </ul>
      <%= form_tag search_movies_path, method: :get, class: "form-inline", role: "search" do %>
        <p>
          <%= text_field_tag :search, params[:search], class: "form-control" %>
          <%= submit_tag "Search", name: nil, class: "btn btn-default" %>
        </p>
      <% end %>
      </div>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>